# Java 并行程序设计
```text
并行程序设计模式属于设计优化的一部分，它是对一些常用的多线程结构的总结和抽象。与串行程序相比，并行程序的结构通常更为复杂。
因此，合理地使用并行模式在多线程开发中具有积极的意义。

在传统的单线程环境下，调用函数是同步的，也就是说它必须等到服务程序返回结果后，才能进行其他处理。
而在Future模式下，调用方式则改为异步，原先等待返回的时间段可用于在主调函数中处理其他事务

```
